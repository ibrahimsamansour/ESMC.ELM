@model IEnumerable<ESMC.ELM.Domain.Entities.Requirement>

@{
    ViewData["Title"] = "Requirements";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h2>Requirements</h2>
        <p class="text-muted mb-0">
            Manage customer and engineering requirements.
        </p>
    </div>

    <a asp-action="Create"
       class="btn btn-primary">
        Add Requirement
    </a>
</div>

<div class="card">
    <div class="card-body">

        @if (!Model.Any())
        {
            <div class="alert alert-info mb-0">
                No requirements have been added yet.
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead>
                        <tr>
                            <th>Code</th>
                            <th>Title</th>
                            <th>Project</th>
                            <th>Type</th>
                            <th>Priority</th>
                            <th>Status</th>
                            <th style="width: 160px;">Actions</th>
                        </tr>
                    </thead>

                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <strong>
                                        @item.RequirementCode
                                    </strong>
                                </td>

                                <td>
                                    @item.Title
                                </td>

                                <td>
                                    @if (item.Project != null)
                                    {
                                        <strong>
                                            @item.Project.ProjectCode
                                        </strong>

                                        <div class="small text-muted">
                                            @item.Project.ProjectName
                                        </div>
                                    }
                                    else
                                    {
                                        <span class="text-muted">
                                            General
                                        </span>
                                    }
                                </td>

                                <td>
                                    @item.RequirementType
                                </td>

                                <td>
                                    @if (string.Equals(
                                                                item.Priority,
                                                                "Critical",
                                                                StringComparison.OrdinalIgnoreCase))
                                    {
                                        <span class="badge bg-danger">
                                            Critical
                                        </span>
                                    }
                                    else if (string.Equals(
                                    item.Priority,
                                    "High",
                                    StringComparison.OrdinalIgnoreCase))
                                    {
                                        <span class="badge bg-warning text-dark">
                                            High
                                        </span>
                                    }
                                    else if (string.Equals(
                                    item.Priority,
                                    "Medium",
                                    StringComparison.OrdinalIgnoreCase))
                                    {
                                        <span class="badge bg-info text-dark">
                                            Medium
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary">
                                            @item.Priority
                                        </span>
                                    }
                                </td>

                                <td>
                                    @if (string.Equals(
                                                                item.Status,
                                                                "Approved",
                                                                StringComparison.OrdinalIgnoreCase))
                                    {
                                        <span class="badge bg-success">
                                            Approved
                                        </span>
                                    }
                                    else if (string.Equals(
                                    item.Status,
                                    "Draft",
                                    StringComparison.OrdinalIgnoreCase))
                                    {
                                        <span class="badge bg-secondary">
                                            Draft
                                        </span>
                                    }
                                    else if (string.Equals(
                                    item.Status,
                                    "Rejected",
                                    StringComparison.OrdinalIgnoreCase))
                                    {
                                        <span class="badge bg-danger">
                                            Rejected
                                        </span>
                                    }
                                    else if (string.Equals(
                                    item.Status,
                                    "Obsolete",
                                    StringComparison.OrdinalIgnoreCase))
                                    {
                                        <span class="badge bg-warning text-dark">
                                            Obsolete
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-dark">
                                            @item.Status
                                        </span>
                                    }
                                </td>

                                <td>
                                    <div class="d-flex gap-1">

                                        <a asp-action="Details"
                                           asp-route-id="@item.RequirementId"
                                           class="btn btn-sm btn-outline-primary">
                                            Details
                                        </a>

                                        <a asp-action="Edit"
                                           asp-route-id="@item.RequirementId"
                                           class="btn btn-sm btn-outline-secondary">
                                            Edit
                                        </a>

                                        <a asp-action="Functions"
                                           asp-route-id="@item.RequirementId"
                                           class="btn btn-sm btn-outline-success">
                                            Functions
                                        </a>

                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }

    </div>
</div>